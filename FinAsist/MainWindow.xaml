<Window x:Class="FinAsist.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
        Title="NexFin AI - Akıllı Finans Asistanı" Height="700" Width="1000" WindowStartupLocation="CenterScreen"
        Background="#121212">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="320"/>
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,20">
            <Button Content="📁 Ekstre Yükle" Width="150" Height="45" Background="#1E1E1E" Foreground="#00E676" BorderBrush="#00E676" BorderThickness="1" FontWeight="Bold" Cursor="Hand" Click="BtnEkstreYukle_Click"/>
            <Button Content="🤖 AI Asistan &amp; Özet" Width="160" Height="45" Margin="15,0,0,0" Background="#00E676" Foreground="#121212" FontWeight="Bold" Cursor="Hand" Click="BtnAsistan_Click"/>
            <Button Content="📄 PDF Çıktısı Al" Width="150" Height="45" Margin="15,0,0,0" Background="#1E1E1E" Foreground="#00BFFF" BorderBrush="#00BFFF" BorderThickness="1" FontWeight="Bold" Cursor="Hand" Click="BtnPdfIndir_Click"/>
        </StackPanel>

        <DataGrid x:Name="GridHarcamalar" Grid.Row="1" Grid.Column="0" AutoGenerateColumns="False" IsReadOnly="False" 
                  Margin="0,0,15,0" Background="#1E1E1E" RowBackground="#1E1E1E" AlternatingRowBackground="#252526" 
                  Foreground="#E0E0E0" BorderBrush="#333333" BorderThickness="1" GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#333333">

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#252526"/>
                    <Setter Property="Foreground" Value="#00E676"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="Padding" Value="10,5"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#00E676"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Tarih" Binding="{Binding Tarih}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Açıklama" Binding="{Binding Aciklama}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Kategori" Binding="{Binding Kategori}" Width="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Foreground" Value="#00E676"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Tutar (TL)" Binding="{Binding Tutar, StringFormat={}{0:N2}}" Width="120" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" Padding="20" CornerRadius="8">
                <StackPanel>
                    <TextBlock Text="🎯 Hedef Bütçe (TL)" Foreground="#00E676" FontWeight="Bold" FontSize="16"/>
                    <TextBox x:Name="TxtHedefButce" Text="5000" Margin="0,10,0,20" Padding="8" Background="#121212" Foreground="White" BorderBrush="#00E676" FontSize="14"/>

                    <TextBlock Text="📢 AI Durum Raporu" Foreground="#00E676" FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                    <TextBlock x:Name="TxtAsistanMesaj" TextWrapping="Wrap" Text="Sistem hazır. Analiz için ekstre bekleniyor..." Foreground="#B0B0B0" FontSize="13" LineHeight="18"/>
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Margin="0,15,0,0" Background="#1E1E1E" BorderBrush="#333333" BorderThickness="1" Padding="10" CornerRadius="8">
                <Grid>
                    <TextBlock Text="📊 Harcama Dağılımı" Foreground="#00E676" FontWeight="Bold" FontSize="16" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                    <lvc:PieChart x:Name="ChartHarcamalar" InnerRadius="40" LegendLocation="Bottom" Hoverable="True" Margin="0,30,0,0">
                        <lvc:PieChart.ChartLegend>
                            <lvc:DefaultLegend Foreground="#E0E0E0" />
                        </lvc:PieChart.ChartLegend>
                    </lvc:PieChart>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Grid.ColumnSpan="2" Background="#00E676" Padding="15" Margin="0,20,0,0" CornerRadius="5">
            <TextBlock x:Name="TxtToplamHarcama" Text="Toplam Harcama: 0.00 TL" FontSize="22" FontWeight="ExtraBold" Foreground="#121212" HorizontalAlignment="Right"/>
        </Border>
    </Grid>
</Window>